import React from "react";
import { TopBar } from "@shopify/polaris";

const TopBarMenu = () => {
  return <TopBar showNavigationToggle onNavigationToggle={() => {}} />;
};

export default TopBarMenu;
